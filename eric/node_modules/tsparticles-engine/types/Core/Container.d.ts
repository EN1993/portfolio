import { Canvas } from "tsparticles-engine/types/Core/Canvas";
import type { ClickMode } from "tsparticles-engine/types/Enums/Modes/ClickMode";
import type { Engine } from "tsparticles-engine/types/engine";
import { FrameManager } from "tsparticles-engine/types/Core/Utils/FrameManager";
import type { IContainerInteractivity } from "tsparticles-engine/types/Core/Interfaces/IContainerInteractivity";
import type { IContainerPlugin } from "tsparticles-engine/types/Core/Interfaces/IContainerPlugin";
import type { IMovePathGenerator } from "tsparticles-engine/types/Core/Interfaces/IMovePathGenerator";
import type { IOptions } from "tsparticles-engine/types/Options/Interfaces/IOptions";
import type { IShapeDrawer } from "tsparticles-engine/types/Core/Interfaces/IShapeDrawer";
import { Options } from "tsparticles-engine/types/Options/Classes/Options";
import type { Particle } from "tsparticles-engine/types/Core/Particle";
import { Particles } from "tsparticles-engine/types/Core/Particles";
import type { RecursivePartial } from "tsparticles-engine/types/Types/RecursivePartial";
import { Retina } from "tsparticles-engine/types/Core/Retina";
import type { Vector } from "tsparticles-engine/types/Core/Utils/Vector";
export declare class Container {
    readonly id: string;
    actualOptions: Options;
    readonly canvas: Canvas;
    destroyed: boolean;
    readonly drawers: Map<string, IShapeDrawer>;
    duration: number;
    fpsLimit: number;
    readonly frameManager: FrameManager;
    interactivity: IContainerInteractivity;
    lastFrameTime?: number;
    lifeTime: number;
    pageHidden: boolean;
    readonly particles: Particles;
    pathGenerators: Map<string, IMovePathGenerator>;
    readonly plugins: Map<string, IContainerPlugin>;
    responsiveMaxWidth?: number;
    readonly retina: Retina;
    smooth: boolean;
    started: boolean;
    zLayers: number;
    private _currentTheme?;
    private _delay;
    private _delayTimeout?;
    private _drawAnimationFrame?;
    private readonly _engine;
    private readonly _eventListeners;
    private _firstStart;
    private readonly _initialSourceOptions;
    private readonly _intersectionObserver;
    private _options;
    private _paused;
    private _sourceOptions;
    constructor(engine: Engine, id: string, sourceOptions?: RecursivePartial<IOptions>);
    get options(): Options;
    get sourceOptions(): RecursivePartial<IOptions> | undefined;
    addClickHandler(callback: (evt: Event, particles?: Particle[]) => void): void;
    addPath(key: string, generator?: IMovePathGenerator, override?: boolean): boolean;
    destroy(): void;
    draw(force: boolean): void;
    exportConfiguration(): string;
    exportImage(callback: BlobCallback, type?: string, quality?: number): void;
    exportImg(callback: BlobCallback): void;
    getAnimationStatus(): boolean;
    handleClickMode(mode: ClickMode | string): void;
    init(): Promise<void>;
    loadTheme(name?: string): Promise<void>;
    pause(): void;
    play(force?: boolean): void;
    refresh(): Promise<void>;
    reset(): Promise<void>;
    setNoise(noiseOrGenerator?: IMovePathGenerator | ((particle: Particle) => Vector), init?: () => void, update?: () => void): void;
    setPath(pathOrGenerator?: IMovePathGenerator | ((particle: Particle) => Vector), init?: () => void, update?: () => void): void;
    start(): Promise<void>;
    stop(): void;
    updateActualOptions(): boolean;
    private _intersectionManager;
}
