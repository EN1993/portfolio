import type { ICoordinates, IRangedCoordinates } from "tsparticles-engine/types/Core/Interfaces/ICoordinates";
import type { IDimension } from "tsparticles-engine/types/Core/Interfaces/IDimension";
export interface IPositionFromSizeParams {
    position?: Partial<ICoordinates>;
    size: IDimension;
}
export interface IRangedPositionFromSizeParams {
    position?: Partial<IRangedCoordinates>;
    size: IDimension;
}
