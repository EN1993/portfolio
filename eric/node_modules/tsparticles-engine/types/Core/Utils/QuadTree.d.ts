import type { ICoordinates } from "tsparticles-engine/types/Core/Interfaces/ICoordinates";
import type { IDimension } from "tsparticles-engine/types/Core/Interfaces/IDimension";
import type { Particle } from "tsparticles-engine/types/Core/Particle";
import type { Point } from "tsparticles-engine/types/Core/Utils/Point";
import type { Range } from "tsparticles-engine/types/Core/Utils/Range";
import { Rectangle } from "tsparticles-engine/types/Core/Utils/Rectangle";
export declare class QuadTree {
    readonly rectangle: Rectangle;
    readonly capacity: number;
    private _NE?;
    private _NW?;
    private _SE?;
    private _SW?;
    private _divided;
    private readonly _points;
    constructor(rectangle: Rectangle, capacity: number);
    insert(point: Point): boolean;
    query(range: Range, check?: (particle: Particle) => boolean, found?: Particle[]): Particle[];
    queryCircle(position: ICoordinates, radius: number, check?: (particle: Particle) => boolean): Particle[];
    queryRectangle(position: ICoordinates, size: IDimension, check?: (particle: Particle) => boolean): Particle[];
    private subdivide;
}
