import type { IColor, IRangeColor, IRgb, IRgba } from "tsparticles-engine/types/Core/Interfaces/Colors";
import type { IColorManager } from "tsparticles-engine/types/Core/Interfaces/IColorManager";
export declare class HslColorManager implements IColorManager {
    readonly key: string;
    readonly stringPrefix: string;
    constructor();
    handleColor(color: IColor): IRgb | undefined;
    handleRangeColor(color: IRangeColor): IRgb | undefined;
    parseString(input: string): IRgba | undefined;
}
